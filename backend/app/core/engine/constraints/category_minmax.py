# Placeholder for your per-impairment/category min/max.
# You can later compute or load from JSON.
CATEGORY_MINMAX = {
    # Example keys; in real use pick a table keyed by impairment blend
    "default": {
        "font_size": (10, 22),
        "target_size": (20, 44),
    }
}
