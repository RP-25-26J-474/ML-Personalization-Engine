name: Docker Image CI/CD (ML Personalization Engine)

on:
  push:
    branches: [ "deploy" ]
    paths:
      - 'backend/**'
      - '.github/workflows/docker-publish.yml'
  pull_request:
    branches: [ "deploy" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push ML backend image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ secrets.DOCKER_USERNAME }}/aura-ml-backend:latest
        cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/aura-ml-backend:buildcache
        cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/aura-ml-backend:buildcache,mode=max

  deploy-to-vps:
    if: ${{ github.event_name != 'pull_request' }}
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
    - name: Deploy ML backend to VPS
      uses: appleboy/ssh-action@v1.2.0
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        # passphrase: ${{ secrets.VPS_SSH_PASSPHRASE }}
        port: ${{ secrets.VPS_PORT || '22' }}
        script_stop: true
        script: |
          set -eu

          LOCK_DIR=/tmp/aura-deploy.lock
          while ! mkdir "$LOCK_DIR" 2>/dev/null; do
            sleep 2
          done
          trap 'rmdir "$LOCK_DIR"' EXIT

          check_url() {
            url="$1"
            attempt=1
            while [ "$attempt" -le 12 ]; do
              if command -v curl > /dev/null 2>&1; then
                if curl -fsS "$url" > /dev/null; then
                  return 0
                fi
              elif wget -q -O /dev/null "$url"; then
                return 0
              fi
              attempt=$((attempt + 1))
              sleep 5
            done
            return 1
          }

          resolve_compose() {
            if docker info > /dev/null 2>&1; then
              if command -v docker-compose > /dev/null 2>&1; then
                COMPOSE_BIN="docker-compose"
                return 0
              fi
              if docker compose version > /dev/null 2>&1; then
                COMPOSE_BIN="docker compose"
                return 0
              fi
            fi

            if sudo -n docker info > /dev/null 2>&1; then
              if command -v docker-compose > /dev/null 2>&1 && sudo -n docker-compose version > /dev/null 2>&1; then
                COMPOSE_BIN="sudo -n docker-compose"
                return 0
              fi
              if sudo -n docker compose version > /dev/null 2>&1; then
                COMPOSE_BIN="sudo -n docker compose"
                return 0
              fi
            fi

            echo "Docker daemon is not accessible for this SSH user. Add the user to the docker group or allow passwordless sudo for docker/docker-compose."
            exit 1
          }

          cd "${{ secrets.VPS_DEPLOY_PATH }}"
          export DOCKER_USERNAME="${{ secrets.DOCKER_USERNAME }}"
          resolve_compose

          sh -c "$COMPOSE_BIN pull ml-backend"
          sh -c "$COMPOSE_BIN up -d --force-recreate --no-deps ml-backend"

          check_url "http://127.0.0.1:4040/health" || {
            sh -c "$COMPOSE_BIN ps ml-backend"
            sh -c "$COMPOSE_BIN logs --tail 50 ml-backend"
            exit 1
          }
